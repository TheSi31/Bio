'use client'

import { timelineItems } from '@/data/timeline'
import BioTimeline from '../BioTimeline/BioTimeline';

const History = () => {
    return (
        <section>
            <BioTimeline />
        </section>
    );
}

export default History;